Divisão de tarefas da equipe

Alana Maria:
- Desenvolveu a estrutura do mini banco de dados com deltalake delta-rs;
- Criou a classe da entidade veículo ("veiculo_model.py") com a definição dos atributos utilizando pydantic para validação e tipagem de dados;
- Desenvolveu a lógica do AUTO INCREMENT do atributo id utilizando o arquivo "ultimo_id.seq";
- Desenvolveu o script em Python para povoar o banco de dados deltalake com 1000 registros realistas utilizando a biblioteca Faker e o módulo random;

Clara Cruz:
- Criou a classe principal do mini banco de dados (BancoVeiculo) referente à entidade veículo;
- Desenvolveu as operações completas de CRUD e manutenção (insert, get, list, update, delete, count e vacuum);
- Aplicou a lógica do AUTO INCREMENT na função de insert;
- Implementou otimizações de memória com PyArrow para paginação e filtros de busca nativos no disco, garantindo que a RAM não seja sobrecarregada.

Vitor Nayan:
- Desenvolveu a API definindo as rotas
